# Paper-safe experiment records

This folder is intentionally tracked by Git. After the final Kaggle run, execute:

```bash
python -m scripts.export_paper_results
```

The script copies only small result files here. Large datasets, model checkpoints and videos remain ignored.
